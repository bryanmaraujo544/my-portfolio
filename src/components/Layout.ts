import styled from 'styled-components';

export const Layout = styled.div`
  width: 100%;
  margin: 0 auto;
  max-width: 1720px;
  padding: 0 6.4rem;
  position: relative;
  overflow: hidden;

  @media (max-width: 768px) {
    padding: 0 3.2rem;
  }
`;
